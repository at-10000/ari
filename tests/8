print(1000);
print(2000);

let initial_balance = 5000.00;
let interest_rate = 0.045;
let years = 10;

let total_return = initial_balance * interest_rate * years;
let final_balance = initial_balance + total_return;

print(initial_balance);
print(final_balance);

let player_x = 150.25;
let player_y = -300.50;
let velocity_x = 12.5;
let velocity_y = -9.81;

player_x = player_x + velocity_x;
player_y = player_y + velocity_y;

print(player_x);
print(player_y);

let enemy_x = 200.0;
let enemy_y = -250.0;

let distance_x = player_x - enemy_x;
let distance_y = player_y - enemy_y;

player_x < enemy_x;
player_y > enemy_y;

let is_under_map = player_y <= -500.0;
let is_at_target = player_x >= 1000.0;

print(is_under_map);
print(is_at_target);

let alpha = 1;
let beta = 2;
let gamma = 3;
let delta = 4;

let step_1 = alpha + beta;
let step_2 = gamma * delta;
let step_3 = step_2 / step_1;
let final_score = step_3 - -50.0;

print(final_score);

		
		
		
				
print(99999);
				
		
		

let _sys_status_flag = 1;
let _network_timeout = 30;
let retry_count = 0;

print(_sys_status_flag);

let a1 = 1.1;
let a2 = 2.2;
let a3 = 3.3;
let a4 = 4.4;
let a5 = 5.5;

let sum = a1 + a2 + a3 + a4 + a5;
let average = sum / 5;

print(average);

let max_limit = 50000.0;
let current_load = 49999.9;

current_load < max_limit;
current_load <= max_limit;
current_load = max_limit;
current_load >= max_limit;

print(current_load);

let multiplier = -1.0;
inverse_score = final_score * multiplier;

print(inverse_score);

print(0);

a1.print();

print(a1, a2);

!a1;